# 1. GLOBAL_VARIABLES always upper case
# 2. specify input/out types whenever possible 'def calc_volume(x: int, y: int, z: int) -> int:', even if return type is None
# 3. Use enums instead of strings! Have a look at auto.
# 4. Unit von variablen hinzufügen, e.g. Byte, dollar
# 5. 
