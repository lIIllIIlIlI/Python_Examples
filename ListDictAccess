# Get specific key of dictionary, e.g. 'b'
myDict = {'a':1, 'b':2}
secondKey = list(myDict.keys())[1]

# iteration over list
for index, value in enumerate(myList)

from operator import attrgetter
map(attrgetter('my_attr'), my_list)

# iteration over dict
